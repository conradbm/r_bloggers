import numpy as np # linear algebra
import pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)
from subprocess import check_output
import pandas as pd


fileName = check_output(["ls", "../input"]).decode("utf8").rstrip()
print(fileName)

results=[]
with open("../input/colbert.csv", 'r') as f_in:

    results = list(map(lambda x:x.rstrip(), f_in.readlines()))
    #print(results[0])
    #print
    #print(results[1])
    
container=[]
atom1=[]
atom2=[]
atom3=[]
for thing in results:
    row=[]
    ls1 = thing.split(",")
    row.append(ls1[0])
    row.append(ls1[1])
    row.append(''.join([s for s in ls1[2:]]))
    container.append(row)
df = pd.DataFrame(container[1:], columns=container[0])
df.head()

### next clean up the words by using some of pandas build in helper functions
